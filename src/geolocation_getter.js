/*
    To get the users' geolocation


    We could gather the users' uid that we have stored in a given lobby's collection
    The uids are sorted in the rtdb, so we could probably gather the uids that we need
    from the rtdb much quicker by using a binary search on it.

*/